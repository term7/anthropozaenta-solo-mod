//# open links in new tab
$('a').each(function(){var a=new RegExp('/'+window.location.host+'/');if(!a.test(this.href)){$(this).attr("target","_blank")}});

//# scroll button
const scrollTop = document.getElementById('scrolltop')
window.onload = () => {
    scrollTop.style.visibility = "hidden";
    scrollTop.style.opacity = 0;
}
window.onscroll = () => {
    if (window.scrollY > 555) {
        scrollTop.style.visibility = "visible";
        scrollTop.style.opacity = 1;
    } else {
        scrollTop.style.visibility = "hidden";
        scrollTop.style.opacity = 0;
    }
};

if ( $(window).width() <= 701) {
    $('.gh-burger').click(function(){
        $(this).toggleClass('white');
        $('.gh-head-logo, .gh-author-mastodon, .gh-author-instagram, .gh-contact').toggleClass('invert no_invert');
        $('.gh-head, .gh-head-actions').toggleClass('accent no_accent');

    });
    $('.nav').click(function(){
        $('.gh-head-logo, .gh-author-mastodon, .gh-author-instagram, .gh-contact').toggleClass('invert no_invert');
        $('.gh-head, .gh-head-actions').toggleClass('accent no_accent');
    });
}